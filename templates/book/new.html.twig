{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1>Добавить книгу</h1>
        </div>
        <div class="row">
            {{ include('book/_form.html.twig') }}


        </div>
        <a href="{{ path('book_index') }}" class="btn btn-default btn-form btn-back">Назад к списку</a>
    </div>

{% endblock %}
